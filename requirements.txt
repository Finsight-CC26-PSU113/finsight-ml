# ============================================================
# OCR FinSight — Production Dependencies
# Python: 3.10, 3.11, 3.12
# Default: CPU-only (works everywhere)
# ============================================================

# --- Core: pin numpy first (compatibility critical) ---
numpy>=1.26.0,<2.0

# --- Image processing ---
opencv-python-headless==4.10.0.84
Pillow>=10.0.0,<11.0

# --- ML frameworks ---
tensorflow>=2.15.0,<2.17
# Note: PyTorch optional (only needed if using EasyOCR instead of PaddleOCR)
# torch==2.1.2
# torchvision==0.16.2

# --- OCR Engines ---
# Option 1: PaddleOCR (recommended - faster, more accurate)
paddlepaddle>=2.6.2  # CPU version
paddleocr>=2.6.1.3

# Option 2: EasyOCR (alternative - requires PyTorch)
# easyocr==1.7.1

# --- Data processing ---
pandas>=2.0.0,<2.3
scikit-learn>=1.3.0,<1.6

# --- Web framework (FastAPI) ---
fastapi>=0.104.0,<0.120
uvicorn[standard]>=0.24.0,<0.35
python-multipart>=0.0.6
jinja2>=3.1.0

# --- Utilities ---
tqdm>=4.65.0
requests>=2.31.0
