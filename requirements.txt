# ============================================================
# OCR FinSight — Dependencies (Production / Inference)
# ============================================================
# Cross-platform: Windows, Linux, macOS, Docker
# Python: 3.10, 3.11, 3.12 supported
# Default: CPU-only (works everywhere; install GPU separately if needed)
# ============================================================

# --- Core: pin numpy first (compatibility critical) ---
numpy>=1.26.0,<2.0          # Must be <2.0 for opencv/easyocr compat

# --- Image processing ---
opencv-python-headless==4.10.0.84   # headless = no GUI deps, smaller, server-friendly
Pillow>=10.0.0,<11.0

# --- ML frameworks ---
tensorflow==2.15.1          # Pinned for cross-platform stability (CPU works everywhere)
torch==2.1.2                # CPU build by default (use index URL for CUDA if needed)
torchvision==0.16.2

# --- OCR ---
easyocr==1.7.1

# --- Data processing ---
pandas>=2.0.0,<2.3
scikit-learn>=1.3.0,<1.6

# --- Web framework ---
flask>=3.0.0,<4.0
fastapi>=0.104.0,<0.120
uvicorn[standard]>=0.24.0,<0.35
python-multipart>=0.0.6
jinja2>=3.1.0

# --- Utilities ---
matplotlib>=3.7.0,<4.0
tqdm>=4.65.0
requests>=2.31.0

# --- Dev / labeling (optional, only needed for ground-truth generation) ---
google-generativeai>=0.7.0  # Gemini Vision API
